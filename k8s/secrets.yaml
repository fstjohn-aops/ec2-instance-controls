apiVersion: v1
kind: Secret
metadata:
  name: slack-credentials
type: Opaque
data:
  # Base64 encoded values - replace with actual values if needed
  # If not using Slack, you can delete this secret entirely
  slack-bot-token: <base64-encoded-bot-token>
  slack-signing-secret: <base64-encoded-signing-secret> 