{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "node-terminal",
			"request": "launch",
			"name": "Fastify",
			"command": "npm run docker && npm run dev",
			"cwd": "${workspaceFolder}",
			"timeout": 10000
		}
	]
}
