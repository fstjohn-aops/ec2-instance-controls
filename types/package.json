{
	"publishConfig": {
		"registry": "https://aops-865917575078.d.codeartifact.us-east-1.amazonaws.com/npm/aops/"
	},
	"name": "@aops-trove/fastack-starter-types",
	"version": "0.1.2",
	"main": "src/index.ts",
	"files": [
		"src/index.ts",
		"src/validators.ts",
		"src/validators-from-types.ts"
	],
	"scripts": {
		"==== TODO Moving forward, use Taskfile.yml instead of these scripts ====": "",
		"preinstall": "command -v trove && trove configure-codeartifact || echo 'Trove not found. Please install trove. This can be ignored in a CI context'",
		"generate": "tsx generate-validators.ts",
		"prepublishOnly": "trove configure-codeartifact && npm i && npm run generate",
		"publish-trove": "trove publish",
		"test": "jest",
		"test-watch": "jest --watch",
		"test-ci": "npm run generate && jest"
	},
	"devDependencies": {
		"@sinclair/typebox-codegen": "^0.9.9",
		"jest": "^29.7.0",
		"prettier": "3.2.5",
		"ts-jest": "^29.1.2",
		"tsx": "^4.15.2",
		"typescript": "^5"
	},
	"dependencies": {
		"@aops-trove/fast-server-common": "^1.0.5"
	},
	"peerDependencies": {
		"@sinclair/typebox": "^0.32.35"
	}
}
